jobs:
- job: Build
  pool:
    vmImage: 'Ubuntu 16.04'
  steps:
  - bash: docker build -t manifoldsite . 
    name: Build_docker_image
  - bash: docker tag manifoldsite $(dockerId).azurecr.io/latest:$(imageName)
    name: Retag_docker_image 

- job: Authenticate
  pool:
    vmImage: 'Ubuntu 16.04'
  steps:
  - bash: docker login -u $(dockerId) -p $(dockerPassword) $(dockerid).azurecr.io
    name: Authenticate_ACR
  dependsOn: Build
  condition: succeeded()

- job: Push
  pool:
    vmImage: 'Ubuntu 16.04'
  steps:
  - bash: docker push $(dockerId).azurecr.io/latest:$(imageName)
    name: Push_built_docker_image
  dependsOn: Authenticate 
  condition: succeeded()

# steps:
# - bash: |
#    # Write your commands here
#    # Use the environment variables input below to pass secret variables to this script
#    docker build -t manifoldsite . # include other options to meet your needs
#    docker tag manifoldsite $(dockerId).azurecr.io/latest:$(Build.SourceVersion) 
# - bash: |
#    docker login -u $(dockerId) -p $(dockerPassword) $(dockerid).azurecr.io
#    docker push $(dockerId).azurecr.io/latest:$(Build.SourceVersion) 

# - task: PublishTestResults@2
#   inputs:
#     testRunner: VSTest
#     testResultsFiles: '**/*.trx'